# DefensorAI

## Overview
**DefensorAI** detects phishing domains and URLs using rule-based methods, machine learning (ML), and artificial intelligence (AI).

## Installation
1. Clone the repository.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt